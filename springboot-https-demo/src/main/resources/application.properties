# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=JKS
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/europa.jks
# The password used to generate the certificate
server.ssl.key-store-password=password
# The alias mapped to the certificate
server.ssl.key-alias=europa

# Since we are using `Spring Security` enabled application, let's configure it to accept only HTTPS requests:
# Enable to Accept only https requests
#server.ssl.enabled=true


#TESTING

# CALLING SSL/HTTPS Remote Service

#trust store location
trust.store=classpath:keystore/europa.jks
#trust store password
trust.store.password=password

